# DevBank Frontend

React dashboard for the DevBank capstone.

## Local Run

```powershell
npm install
npm run dev
```

Default URL: `http://localhost:5173`

The frontend reads `VITE_API_BASE_URL` during build. It defaults to `http://localhost:3001`.

